version: 2.1

description: >
  Publish schema changes to a subgraph in one of your federated.
  If the graph exists in the graph registry but the variant does not, a new variant is created on publish.

display:
  home_url: "https://kolors.com.mx"
  source_url: "https://github.com/10xLabs/graphql-federation-orb"
